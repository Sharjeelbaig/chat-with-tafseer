A backend from which we can chat with Quran. It is built on top of the Ollama + Quran API. 

## Usage
To use the Quran chatbot, you can send a POST request to the `/chat` endpoint with a JSON payload containing your message. For example:

```json
{
  "tafseerAuthor": "Ibn Kathir (Abridged)",
  "

